{
  "M0.5-merge_json": {
    "code_name": "json_aggregate_series",
    "input_label_map": {
      "source_jsons": "RESULT-JSON1,RESULT-JSON2,RESULT-POINT"
    },
    "run_params": {
      "add_key": "batch",
      "add_value": "{batch}"
    },
    "output_file_label": "RESULT-JSON",
    "output_pattern": "result.json",
    "tool_type": "single",
    "comment": "### 📦 数据序列聚合器 (Series Aggregator)\n\n将多个分散的 JSON 数据序列文件合并为一个文件，便于统一绘图或处理。支持批量添加自定义属性。\n\n#### 💡 用法示例：\n\n1.  **合并实验数据**: 输入选 `Exp1, Exp2, Exp3`，输出 `All_Exp`。\n2.  **分组标记**: 设置 `add_key='group'`, `add_value='Control'`，合并后的每个序列都会带上 `group:\n    Control` 属性。"
  }
}
{
  "0-Extract_CV_CSV_from_TXT": {
    "code_name": "extract_csv",
    "input_label_map": {
      "source_txt": "CV"
    },
    "run_params": {},
    "output_file_label": "CV-CSV",
    "output_pattern": "{stem}.csv",
    "tool_type": "single",
    "comment": "### 📂 Raw Data Extraction Tool (Extract)\n\nThis is the **entry point** for all data processing. It cleans raw text files (`.txt`, `.csv`) generated by electrochemical workstations (e.g., CHI, Autolab, Biologic) into standard **pure data CSVs**.\n\n#### 🛠️ Features\n\n1. **Smart Header Recognition**: Automatically skips metadata like instrument parameters, dates, and notes, locating the data area directly.\n2. **Unified Format**: Outputs standard CSV regardless of original delimiters (comma, semicolon, tab).\n3. **De-noising**: Removes empty lines and non-numeric lines.\n\n#### 📝 Why is this step needed?\n\nFiles exported by workstations often contain metadata headers that cause errors in plotting software (like Origin) or Python. This tool acts as a **universal adapter**, ensuring clean, unified data streams for subsequent normalization and IR compensation."
  }
}